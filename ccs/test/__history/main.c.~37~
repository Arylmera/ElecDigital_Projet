#include <main.h>

int b_value;

void main()
{
   setup_low_volt_detect(FALSE);
   b_value = 0;
    
   while(true)
   {

   output_b( b_value );
   output_high(PIN_E1);
   delay_ms(500);
   output_low(PIN_E1);
   b_value++;
   
   }

}

