#include <main.h>

void main()
{
   setup_low_volt_detect(FALSE);

   //Example
   
   int b_value = 0;
   int segA_value = 0;
   int segB_value = 0;
   
   while(true)
   {
   
    if (b_value < 10){
      output_b(b_value);
    }
    else {
      
    }
   
   b_value++;
   }

}
